# Parsers

Reserved for parser definitions and later deterministic parser implementations.

Parser core is offline/no-network, never executes source content, preserves and reports unknown structured fields, and emits Parsed Source Representation (PSR).
